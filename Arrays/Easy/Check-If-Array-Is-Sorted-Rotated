class Solution {
public:
    bool check(vector<int>& arr) {
        int greater=0;
        for(int i=0;i<arr.size();i++){
            if(arr[i]>arr[(i+1)%arr.size()]) greater++;
            if(greater>1) return false;
        }
        return true;
    }
};